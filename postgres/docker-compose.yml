version: '3'

services:
  postgres:
    container_name: postgres
    image: postgres:14
    restart: always
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql/data
    environment:
      #POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345

volumes:
  data:
